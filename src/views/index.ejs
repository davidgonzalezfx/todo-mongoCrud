<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title><%= title %></title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>
<body>
	<div class="container">
		<h1 class="text-center"><%= title %></h1>

		<form action="/add" method="post">
			<h3>Agrega una tarea</h3>
			<div class="form-group">
				<input type="text" class="form-control" name="titulo" placeholder="Titulo">
			</div>
			<div class="form-group">
				<textarea class="form-control " rows="1" cols="50" name="descripcion" id="" placeholder="Agregar descripcion"></textarea>
			</div>
			<button class="btn btn-primary">
				Agregar
			</button>
		</form>

		<hr>

		<table class="table table-bordered table-hover">
			<thead>
				<tr>

					<th>#</th>
					<th>Titulo</th>
					<th>Description</th>
					<th>Opciones</th>

				</tr>
			</thead>
			<tbody>
				<% for(var i=0;i < tasks.length;i++) { %>
					<tr>
						<td><%= i+1%></td>
						<td>
							<strong><%= tasks[i].titulo %></strong>
						</td>
						<td>
							<%= tasks[i].descripcion %>
						</td>
						<td>
							<a class="<%= tasks[i].estado ? 'btn btn-success': 'btn btn-light' %>" 
								 href="/turn/<%= tasks[i]._id %>">Hecho</a>

							<a class="btn btn-danger" 
								 href="/delete/<%= tasks[i]._id %>">Eliminar</a>							
						</td>
					</tr>
				<% }%>
			</tbody>
		</table>
	</div>
	<script>
		document.getElementById('titulo').focus();
	</script>
</body>
</html>